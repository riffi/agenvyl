import {describe,expect,it} from 'vitest';
import {buildCodexCatalog,parseCodexPermission} from './mode-catalog.js';
describe('Codex execution catalog',()=>{const models=[{model:'gpt-codex',displayName:'GPT Codex',hidden:false,defaultReasoningEffort:'medium',supportedReasoningEfforts:[{reasoningEffort:'low'},{reasoningEffort:'high'}]},{model:'hidden',displayName:'Hidden',hidden:true,supportedReasoningEfforts:[]}];it('always publishes every Codex permission profile',()=>expect(buildCodexCatalog(models)).toEqual({models:[{id:'gpt-codex',label:'GPT Codex',reasoningEfforts:['low','high'],defaultReasoningEffort:'medium'}],controls:{nativeWorkflowModes:['plan','work'],permissionProfiles:[{id:'workspace-write',label:'Workspace write'},{id:'read-only',label:'Read only'},{id:'danger-full-access',label:'Full access'}],agentVariants:[]}}));it('accepts full access as a regular Codex permission profile',()=>expect(parseCodexPermission('danger-full-access')).toBe('danger-full-access'))});
